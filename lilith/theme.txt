# ══════════════════════════════════════════════════════════════
#  Lilith Linux — Revelation Edition  |  GRUB2 Boot Theme
#  Design: ChatGPT-Lilith_Linux_Boot_Design.pdf
#  Background: sigil in upper 65%, dark area below for panel
#  Panel: 9-slice dark-crimson frosted glass, rounded corners
#  Selection: ember-orange glow strip
#  Icon: purple Lilith sigil (transparent PNG)
# ══════════════════════════════════════════════════════════════

# ── Global ──────────────────────────────────────────────────────
title-text:    ""
desktop-image: "background.jpg"
desktop-color: "#000000"

# ── Boot menu ───────────────────────────────────────────────────
# Background sigil fills the upper ~65%; this panel sits cleanly
# in the dark lower portion, centred horizontally.
#
# At 1024x768:  top=55% → y=422, height=36% → 276px
#   4 entries × 42px + 3 gaps × 4px + 2 × 10px padding = 208px ✓
# At 1920x1080: top=55% → y=594, height=36% → 389px (more room)
+ boot_menu {
  left   = 22%
  top    = 55%
  width  = 56%
  height = 36%

  # 9-slice dark smoked-glass panel with crimson border
  menu_pixmap_style = "panel_*.png"

  # Typography — Terminus for crispness at all sizes
  item_font          = "Terminus Regular 16"
  selected_item_font = "Terminus Bold 16"

  # Text colours
  item_color          = "#c8c8c8"
  selected_item_color = "#ffffff"

  # Purple Lilith sigil icon next to each entry
  icon_width      = 40
  icon_height     = 40
  item_icon_space = 16

  # Row sizing — tight but uncluttered
  item_height  = 42
  item_padding = 10
  item_spacing  = 4

  # Ember-orange glow on selected row
  selected_item_pixmap_style = "select_*.png"

  # Hide scrollbar (clean look, all entries fit)
  scrollbar = false
}

# ── Countdown label ─────────────────────────────────────────────
# Sits just above the panel, subdued crimson tone
+ label {
  top   = 51%
  left  = 22%
  width = 56%
  align = "center"
  id    = "__timeout__"
  text  = "Booting in %d seconds"
  color = "#7a2010"
  font  = "Terminus Regular 14"
}

# ── Footer navigation hints ──────────────────────────────────────
# Subtle single-line bar at the very bottom of screen
+ image {
  top    = 94%
  left   = 50%-400
  width  = 800
  height = 40
  file   = "info.png"
}
